1. @Column(length=50) vs @Size(max=50)
    -@Column은 JPA엔티티 클래스의 필드가 DB컬럼으로 매핑될때 해당 컬럼의 길이를 지정하는 데 사용
    -@Size는 Bean ValidationAPI에서 제공하는 어노테이션으로 문자열 컬렉션 배열 등의 크기를 제약을 설정할 때 사용
        -디비의 스키마와는 관련이 없고 자바 레벨에서의 제약을 설정

    -결론: 디비의 컬럼의 설정을 지정하는데 사용되는 것이니 @Column이 더 적절

