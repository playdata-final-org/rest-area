package com.example.BAS.repository;

import com.example.BAS.entitiy.files.CollectionImages;
import org.springframework.data.jpa.repository.JpaRepository;

public interface CollectionImageRepository extends JpaRepository<CollectionImages,Long> {
}
